import React from 'react'
import './MainSection.css'

const MainSection = () => {
    return (
        <>
            <section className={'hero-container'}>
                <span>What are you waiting for?</span>
                {/*<Link to={'/sign-up'} className='btn btn--outline btn--large buttons'>REGISTER NOW</Link>*/}
                {/*OR*/}
                {/*<Link to={'/login'} className={'btn btn--large btn--primary buttons'}>LOGIN</Link>*/}
            </section>
        </>
    )
}

export default MainSection;
